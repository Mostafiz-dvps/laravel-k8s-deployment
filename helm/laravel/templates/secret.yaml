apiVersion: v1
kind: Secret
metadata:
  name: {{ include "laravel.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "laravel.labels" . | nindent 4 }}
type: Opaque
data:
  APP_KEY: {{ .Values.secret.appKey | b64enc | quote }}
